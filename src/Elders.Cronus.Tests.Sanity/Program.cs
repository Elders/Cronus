using NUnit.Framework;
using System;
using System.Linq;

namespace Elders.Cronus.Tests.Sanity
{
    public class Program
    {
        public static int Main(params string[] args)
        {
            //bool hasErrors = false;
            //Type t = typeof(CronusSanityTests<Program>);
            //var TestFixture = Activator.CreateInstance(t);
            //var tests = t.GetMethods().Where(x => (x.GetCustomAttributes(false).Where(y => (y as TestAttribute) != null)).Count() > 0);

            //(TestFixture as CronusSanityTests<Program>).SetXmlLocation(args[0]);

            //foreach (var testMethod in tests)
            //{
            //    try
            //    {
            //        testMethod.Invoke(TestFixture, new object[] { });
            //    }
            //    catch (Exception ex)
            //    {
            //        Console.WriteLine(ex.InnerException.Message);
            //        hasErrors = true;
            //    }
            //}

            //return hasErrors ? -1 : 0;
            return 0;
        }
    }
}
